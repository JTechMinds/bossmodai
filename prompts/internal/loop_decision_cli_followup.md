Use the BossMod CLI result above for this turn. Respond next with a final JSON decision, or one more distinct CLI lookup if you still need a fact. Do not repeat the same peek. request_host_access is free. If this needs a multi-step review, accept the work instead of more peeks.
