The model call timed out {{parsed_error}} before this turn returned one JSON envelope.

Retry this turn. Return one JSON object, no fences/markdown.

Do not invent Board status. Do not mark the task Done, Blocked, or CLEAR.
